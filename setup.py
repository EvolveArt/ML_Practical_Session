from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='A simple benchmark of classifiers for the MNIST dataset.',
    author='EMSE',
    license='MIT',
)
